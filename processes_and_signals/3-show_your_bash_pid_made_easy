#!/usr/bin/env bash
# Show your Bash PID made easy

# Find all processes with the word "bash" in their name
pids=$(pgrep -f bash)

# Loop through each PID and display the PID and process name
for pid in $pids; do
    name=$(ps -p $pid -o comm=)
    echo "PID: $pid, Process Name: $name"
done
